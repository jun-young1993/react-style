import { DefaultTheme } from "styled-components";
export interface CustomTheme extends DefaultTheme {
    white: string;
    black: string;
    lightGray: string;
    darkGray: string;
    midLightGray: string;
    lightPink: string;
    beige: string;
    lightBlue: string;
}